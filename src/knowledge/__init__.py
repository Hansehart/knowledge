"""Work on one knowledge base: capture a source, name a topic, relate a thing.

A knowledge base is a repository holding a domain modelled in schema.org and SKOS, beside a backup of the
library its sources were read from. Both are text, reviewed in a diff and versioned with everything else,
and this package is what writes them so that neither has to be edited by hand.
"""
